# Квитанции за занятия

Задача фичи — снять с репетитора всю возню с оплатой. Занятие закончилось —
ученику и родителю сама пришла квитанция на его стоимость. Долг висит дольше
заданного срока — само ушло напоминание. Репетитор ничего не выставляет и ни о
чём не напоминает: он только получает деньги и отмечает оплату там же, где
отмечал всегда («Финансы» → «Оплатил», либо оплата приходит через ЮKassa).

## Что где лежит

| Файл | Роль |
|---|---|
| `supabase/lesson_invoices.sql` | таблицы `lesson_invoices` / `tutor_invoice_settings`, выставление, напоминания, расписание pg_cron |
| `src/billing.js` | долг, оплаты и абонементы — общая арифметика для всех экранов |
| `src/invoices.js` | как долг раскладывается по квитанциям |
| `src/components/PaymentModeModal.jsx` | настройка способа оплаты ученика |
| `src/components/AutoInvoiceSettings.jsx` | карточка «Квитанции после занятия» в разделе «Профиль» |
| `src/components/InvoiceCard.jsx` | квитанции в кабинете ученика и родителя |
| `src/pages/invoicePdf.js` | печатная квитанция (PDF) |

## Как это работает

1. Репетитор включает тумблер в разделе «Профиль» → «Квитанции после занятия». В этот момент фиксируется
   дата `since` — квитанции выставляются только за занятия начиная с неё, иначе
   первый запуск выкатил бы ученику полсотни счетов за всю историю.
2. Каждые 10 минут задание pg_cron зовёт `invoices_sync_for(null)`. Функция
   разворачивает `students.lessons`, берёт занятия, которые уже закончились
   (плюс выбранная задержка), и вставляет по строке в `lesson_invoices` — с
   `on conflict do nothing`, поэтому повторный запуск ничего не дублирует.
3. Тем же запросом ученику пишется уведомление в `notifications` (тот же
   колокольчик, что и для ДЗ). У родителя своего аккаунта нет — он видит
   квитанции в кабинете по коду.
4. Раз в сутки `invoices_remind()` смотрит, у кого долг висит дольше
   `remind_days`, и шлёт ОДНО напоминание на ученика (не по одному на занятие).

Кабинет дополнительно дёргает `invoices_sync_self()` / `invoices_sync_mine()`
при открытии — страховка на случай, если pg_cron на базе недоступен.

## Почему квитанция не хранит «оплачено»

Долг во всём приложении считает одна функция — `studentDebt()` из
`src/billing.js`: **начислено − оплачено**, где начисление зависит от способа
оплаты (см. ниже про абонемент). Так его показывают «Финансы», карточка
ученика, кабинет ученика, кабинет родителя и телеграм-бот. Если бы у квитанции
был собственный флаг «оплачено», он разъезжался бы с этим числом при правке
цены, удалении занятия или платеже задним числом — и ученик видел бы одно, а
репетитор другое.

Поэтому квитанция фиксирует только факт «за это занятие выставлен счёт на такую
сумму», а непогашенный долг раскладывается по квитанциям от новых к старым
(`withPaymentState` в `src/invoices.js`): свежие занятия ждут оплаты, старые уже
закрыты. Это ровно тот же порядок, в котором `getUnpaidLessons()` показывает
репетитору неоплаченные занятия, — просто с другой стороны.

Из этого следует: **никаких действий при оплате не нужно**. Репетитор отметил
платёж (или пришла оплата из ЮKassa) — квитанции погасились сами.

## Абонемент — настройка ученика, а не отдельные деньги

Ученик может платить не за каждое занятие, а сразу за неделю, две недели или
месяц. Это **способ оплаты в карточке ученика** (`payment_mode`,
`package_period`, `package_start`, `package_amount` — миграция
`supabase/student_payment_mode.sql`), а не отдельная сущность: своей записи в
`payments`, своего статуса «оплачен» и «остатка занятий» у абонемента нет.

Денежный поток остаётся один: **начислено − оплачено = долг**. Абонемент меняет
только момент начисления:

| Способ | Когда начисляется долг |
|---|---|
| За занятие | по мере проведения занятий |
| Абонементом | за весь период сразу, как только период начался |

**Период абонемента меряется занятиями, а не датами.** «Месяц» у ученика,
который занимается дважды в неделю, — это восемь занятий; трижды в неделю —
двенадцать. Периоды идут подряд кусками списка занятий по N штук, где
N = «занятий в неделю × недель в периоде» (1 / 2 / 4), а занятия в неделю
считаются по расписанию — повторяющимся парам «день недели + время»
(`weeklyRate` и `packageSize` в `src/billing.js`, зеркала `package_rate()` и
`package_size()` в базе). Период начался, когда пришёл день его первого
занятия; его границы — даты первого и последнего занятия куска.

Календарное окно, которое было здесь раньше, давало другое число: тридцать
дней с 4 сентября накрывают пять суббот и разовое занятие в пятницу, и «месяц»
молча превращался в десять занятий вместо восьми. Разовая встреча, которой нет
в расписании, теперь в норму недели не входит, но в оплачиваемый период
попадает — она такое же занятие, как остальные.

Оплата у обоих одинаковая — обычная запись в `students.payments`, которую
репетитор вносит в «Финансах». У абонементного ученика она просто закрывает
несколько занятий разом.

Считает всё `accrualEntries()` в `src/billing.js`. Занятия до даты начала
абонемента начисляются по-старому, по факту проведения, — иначе переход на
абонемент задним числом начислил бы то, о чём не договаривались.

**Сумму за период можно вписать руками** (поле «Сумма за период» в модалке
«Как платит ученик»). Расчёт «занятий в периоде × цена» — догадка по
расписанию, а договариваются о круглом числе: скидка за оплату вперёд,
«столько в месяц независимо от переносов». Пустое поле означает «считать по
расписанию», расчётная сумма стоит в нём подсказкой.

Вписанная сумма второго потока денег не заводит: начисление за период
раскладывается по его занятиям (остаток от деления — первому), поэтому
квитанции, список неоплаченных занятий и кабинет родителя сходятся с долгом до
рубля. Период **без** занятий не стоит ничего даже при вписанной сумме: деньги,
не привязанные ни к одному занятию, не легли бы ни в одну квитанцию.

**Двух денежных сущностей быть не должно.** Первая версия этой фичи хранила
абонемент отдельным платежом со своим статусом оплаты — в итоге у одного ученика
на экране жили два разных числа (долг и «ждёт оплаты»), и он попадал сразу в два
списка на «Финансах». Это оказалось непонятно и было отменено.

## Что видит плательщик

* Сумму к оплате и список квитанций со статусами — в кабинете (вкладка
  «Оплата» у ученика, отдельный блок у родителя).
* Реквизиты для перевода, если репетитор их заполнил, — с кнопкой копирования.
* PDF-квитанцию: одну за занятие или сводную за все неоплаченные сразу.
* Кнопку онлайн-оплаты, если репетитор подключил ЮKassa (`docs/yookassa.md`) —
  это отдельная фича, квитанции работают и без неё.

## Занятие не состоялось

Занятие, снятое со счёта кнопкой «не состоялось» (`status: excused`), счёта не
получает вовсе, а уже выставленный аннулируется при ближайшей синхронизации —
как и счёт за занятие, которое убрали из расписания. Репетитор может
аннулировать квитанцию и руками, крестиком в журнале карточки «Квитанции после
занятия» (`canceled_at`), — она исчезает у ученика.

Аннулирование идёт строго по отсутствию занятия в карточке, а НЕ по нулевому
начислению: стёртая цена занятия иначе погасила бы всю историю счетов разом.

## Подводные камни

* **Время занятий — московское, база живёт в UTC.** Конец занятия считает
  `lesson_end_utc()` с явной зоной `Europe/Moscow`; без неё вечерние занятия
  уезжали бы в следующий день.
* **pg_cron ставится без перезапуска базы**, потому что он уже прописан в
  `shared_preload_libraries` образа supabase/postgres. Если расширения не
  окажется, миграция не падает — остаётся выставление при заходе в кабинет.
* **Сумму квитанции считает `student_accrual()` — SQL-зеркало
  `accrualEntries()` из `src/billing.js`.** Это одно правило на двух языках, и
  расходиться им нельзя: кабинет по нему показывает долг, база по нему
  выставляет счета. `package_rate()` и `package_size()` повторяют `weeklyRate()`
  и `packageSize()`, разбиение списка занятий на периоды — то же деление с
  остатком (`i / size`), а доля занятия в периоде — то же деление суммы с
  остатком на первое занятие. Правя одну сторону, правь вторую и прогоняй
  сверку: 26 случаев JS против SQL, расхождений быть не должно.
* **Сумма не зависит от длительности занятия** — так же, как её считает долг.
  Иначе полуторачасовое занятие давало бы квитанцию на одну сумму, а долг на
  другую.
* **Синхронизация подтягивает уже выставленные квитанции под начисление.**
  Репетитор мог поменять цену или сумму абонемента задним числом; своей истины
  у квитанции нет, поэтому сумма переписывается, а не остаётся исторической.
* **Выставлено всегда не больше, чем начислено.** Счёт приходит после занятия,
  а абонемент начисляет весь период вперёд — поэтому в середине периода
  квитанций меньше, чем долга. Это нормально: долг показывает раздел «Оплата»,
  а квитанции гасятся снизу вверх.
* Ученик без строки в `student_accounts` (репетитор завёл его вручную и тот
  никогда не входил) квитанции получит, но уведомление ему отправить некуда —
  их увидит родитель по коду.
