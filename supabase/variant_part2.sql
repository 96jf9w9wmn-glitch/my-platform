-- Часть 2 в вариантах: ученик выбирает ответ из четырёх предложенных (сам выбор
-- хранится здесь), обязательно прикрепляет фото решения (part2_files, колонка уже есть),
-- а баллы за часть 2 начисляет только репетитор при проверке (part2_score_detail).
-- Выполнить в Supabase → SQL Editor. Фронтенд устойчив к отсутствию колонки
-- (сохранит хотя бы часть 1), но выбор ученика без неё теряется — выполнить до деплоя.

alter table variant_submissions
  add column if not exists part2_choices jsonb;

comment on column variant_submissions.part2_choices is
  'Выбранные учеником ответы части 2: { "20": "−4; 1", ... }. Балл ставит репетитор.';
